name: Discord Log Auto-Updater

on:
  workflow_dispatch:
    inputs:
      lookback_days:
        description: 'Number of days to go back'
        required: true
        default: '7'

jobs:
  update_logs:
    runs-on: ubuntu-latest
    
    steps:
      - name: Setup Tools
        run: |
          curl -L https://github.com/Tyrrrz/DiscordChatExporter/releases/latest/download/DiscordChatExporter.Cli.linux-x64.zip -o dce.zip
          unzip dce.zip -d dce && chmod +x dce/DiscordChatExporter.Cli

      - name: Install Pandas
        run: pip install pandas

      - name: Get Dropbox Token
        id: dropbox_auth
        run: |
          TOKEN=$(curl -s -X POST https://api.dropbox.com/oauth2/token \
            -u "${{ secrets.DROPBOX_APP_KEY }}:${{ secrets.DROPBOX_APP_SECRET }}" \
            -d grant_type=refresh_token \
            -d refresh_token="${{ secrets.DROPBOX_REFRESH_TOKEN }}" | jq -r '.access_token')
          echo "atoken=$TOKEN" >> $GITHUB_OUTPUT

# ... (previous steps: Setup, Install, Get Token) ...

      - name: Download, Merge & Priority Sort
        run: |
          curl -L "${{ secrets.DROPBOXLINK }}" -o master_history.csv

          LOOKBACK_DATE=$(date -d "${{ github.event.inputs.lookback_days }} days ago" +%Y-%m-%d)
          
          ./dce/DiscordChatExporter.Cli export \
            -t "${{ secrets.DISCORD_TOKEN }}" \
            -c ${{ secrets.CHANNEL_ID }} \
            -f Csv -o recent_slice.csv \
            --after "$LOOKBACK_DATE"

          python3 -c "
          import pandas as pd
          try:
              # Force all columns to strings to prevent the 'str vs float' merge error
              # Using header=None for Master, header=0 for New Export (DCE always adds header)
              master = pd.read_csv('master_history.csv', header=None, dtype=str, low_memory=False)
              recent = pd.read_csv('recent_slice.csv', header=0, dtype=str, low_memory=False)
              
              # Standardize column count to 4 (UserID, Username, Timestamp, Message)
              # This removes any accidental 5th/empty columns from either file
              master = master.iloc[:, :4]
              recent = recent.iloc[:, :4]
              
              cols = ['userID', 'userName', 'timestamp', 'message']
              master.columns = cols
              recent.columns = cols

              # --- PRIORITY MERGE ---
              # We use userID and timestamp as the unique key
              key = ['userID', 'timestamp']
              
              # Identify rows in master that are NOT in the recent export
              # This 'clears the way' for the new, potentially fixed messages
              master_clean = master.merge(recent[key], on=key, how='left', indicator=True)
              master_clean = master_clean[master_clean['_merge'] == 'left_only'].drop(columns=['_merge'])
              
              # Combine and Sort
              final = pd.concat([master_clean, recent], ignore_index=True)
              
              # Convert to datetime only for sorting purposes
              final['dt_sort'] = pd.to_datetime(final['timestamp'], errors='coerce', utc=True)
              final = final.dropna(subset=['dt_sort']).sort_values('dt_sort')
              final = final.drop(columns=['dt_sort'])

              # Save with 4 columns, no header, no index
              final.to_csv('test_merge.csv', index=False, header=False)
              print(f'Merge successful. Final row count: {len(final)}')

          except Exception as e:
              print(f'Critical Error: {e}')
              exit(1)
          "

      - name: Upload to Dropbox (TEST)
        run: |
          curl -X POST https://content.dropboxapi.com/2/files/upload \
            --header "Authorization: Bearer ${{ steps.dropbox_auth.outputs.atoken }}" \
            --header "Dropbox-API-Arg: {\"path\": \"/bruh/bruh_log/test_merge.csv\",\"mode\": \"overwrite\"}" \
            --header "Content-Type: application/octet-stream" \
            --data-binary @test_merge.csv
